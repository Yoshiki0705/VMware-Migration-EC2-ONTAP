# Probe strings this repository registers against sibling repositories.
#
# Format: <repo>\t<cited path>\t<role>\t<probe>, sorted. One line per cited string. The probe is the
# last field and is taken whole, so it may contain anything except a tab. `#` starts a comment only at
# the beginning of a line.
#
# Matching the format S3-Burst-on-ONTAP-Files and FSx-for-ONTAP-Adoption-Playbook publish, so a
# reader written for one works for all three.
#
# Verified by `make outgoing-probes`, which reads each cited repository and fails when a registered
# string is gone. Hand-maintained: a probe is a *chosen* claim-bearing substring, and there is
# nothing to derive it from.
#
# role:
#   retraction  Expected to survive. Its absence means the claim moved or was retracted, and a claim
#               here has lost its evidence. Fails.
#   reread      Expected to move, because it pins a value over a set that grows. Warns.
#
# WHY THIS FILE IS NOT ENOUGH ON ITS OWN.
#
# S3-Burst-on-ONTAP-Files has the mirror check (`make incoming-probes`), which reads a sibling's
# contract and fails when this repository's cited string is reworded there. It was wired to one
# sibling only when this file was written. **That is no longer true**: they tabled the sibling list
# and registered this repository in their #155, and they report 45 probes across 2 siblings verified,
# plus a negative test -- rewording one registered string failed the gate naming sibling, file and
# string, while the other 40 passed.
#
# `make outgoing-probes` here still reads their repository directly, and stays. **The protection this
# side needs is on this side**: their gate fires on their commits, which is where a rewording is
# cheapest to catch, but a gate owned by someone else is not a dependency this file takes on.
#
# TWO LIMITS, DOCUMENTED BY THE OWNER OF THESE STRINGS AND COPIED HERE BECAUSE THEY APPLY EQUALLY:
#
#   1. A probe passes when the string survives anywhere in the file, not where the claim is. A probe
#      on a string that appears several times is weaker than one on a unique string. Every probe
#      below was checked to occur exactly once at the pinned commit.
#   2. A surviving string only says the sentence still exists. A claim retracted but kept as history
#      keeps its probe green. **Registration is a floor, not a substitute for reading.**
#
# Line breaks matter: a probe spanning one never matches. `上限は 1 フローの容量ではない` was
# rejected as a candidate for exactly that reason and the two strings around it registered instead.
# **They later recommended that same string as a replacement, describing it as occurring once on one
# line. Checked at 1bedd45: zero occurrences.** It is still split, now across lines 957-958 of their
# results file (`**上限は 1 フローの` / `容量ではない。**`), and `git grep` over their whole docs tree
# finds it nowhere. Their other suggestion, the already-registered division string, is what stands.
#
# ---------------------------------------------------------------------------------------------------
# FSx-for-ONTAP-Adoption-Playbook
#
# Read at 0723101. Two probes, both on the same file, because **that file corrected this repository's
# FlexClone write-up.** The capability document here framed clone-and-test as testing against
# production data, without recording that a clone of a LUN is crash-consistent and carries no
# application-level guarantee -- `fsck` or `chkdsk` may run when the clone is mounted. Their note also
# states that what FlexClone removes is the impact on production, not the copy itself, since a
# host-mediated route still produces one copy.
#
# If either string goes, the caveat this repository now carries has lost its source and the FlexClone
# section needs rereading before it keeps claiming a clone is production-equivalent.
#
# They register one probe against this repository in the other direction
# (`承認を得て実行` in docs/ja/atx-fsxn-ga-verification.md). **That string occurs three times here**,
# which by their own contract's standard is weak -- rewording one of the three leaves it green. Raised
# with them rather than silently changed.
#
# ---------------------------------------------------------------------------------------------------
# S3-Burst-on-ONTAP-Files
#
# Read at a97a4ec (bumped from 1bedd45; the diff touching cited files is 2 files / 7 lines and the
# anchors still resolve). **F-1, F-2 and F-3 are all measured** and their environments are torn down. The
# earlier note here, that F-2 and F-3 were not run, was true at f27363c and is kept only as the
# reason the probe set was extended rather than written at once.
#
# Repinned from 5d8c28a after their #164 rebuilt the comparison table: the ratio column became a
# measurement-count column, the NVMe/TCP cells became observed ranges, and a note now says the ratio
# is not written because numerator and denominator come from different file systems. **Nothing here
# had to be withdrawn** -- this side had already declined to quote a ratio for that reason, and the
# interval their note now gives (1.14-3.00x) is the one derived here independently.
#
# One claim of theirs is restated in this repository rather than pointed at: that deriving a session
# count by dividing required bandwidth by 625 MBps does not hold. It appears in the blog draft for
# part 1 and in docs/images/architecture.md, and it is the reason the sentence there no longer
# implies such a derivation is written down somewhere here.
#
# Eight probes. The fourth carries an *absence* -- that the cause of the single flow exceeding 5 Gbps
# is not established. If that absence stops being true they will have found the cause, which is
# progress, and it is the case where this side most needs to reread rather than keep citing an
# unexplained number. The fifth is the restriction that keeps the citation honest: their figures are
# raw-device, and this project puts a filesystem on the LUN.
#
# Three more added after F-2 and F-3 (read at 5d8c28a, each occurring once and on one line):
#
#   - that the protocol order reverses between "one connection" and the documented procedure. This
#     is the evidence for keeping this repository's wording that a protocol is not chosen on
#     throughput. **Not that one is faster** -- the finding is that the order changes with the
#     procedure and the I/O shape.
#   - that requesting 36 queues still yielded 4, which is the mechanism itself rather than a value.
#   - that the same configuration spread 2.64x across environments. This is the reason no single
#     figure is quoted as the throughput of a configuration, and it is the claim most likely to be
#     cited by a reader sizing a migration.
#
# `なぜかは確認していない` was **withdrawn**: their #164 additions made it occur twice in that file
# (lines 963 and 1080), and a probe on a repeated string passes while one of the two is reworded.
# Both places carry the same absence, so it is replaced by one unique single-line string from each:
# `どれが当たっているかを検証していないので、原因を書かない` (the F-1 note that names the absence) and
# `9.08 Gbps を運んでおり、同じ上限には当たっていない` (the same absence in the comparison section).
# Rewording either now fails rather than half-failing silently.
#
# `分母と分子が別のファイルシステムから来ており` is registered as `reread`, not `retraction`. It is the
# sentence that licenses this repository's refusal to quote a protocol ratio. If it goes, they have
# most likely measured both protocols on one file system, and the ratio becomes citable -- a reason to
# reread, not a breakage.
#
# **It replaced `倍率を書かない`, which they flagged as about to become weak.** Their #168 added
# `倍率は書かない` to the same file, one particle away, so an edit aligning either to the other would
# have made this probe match twice and weaken in silence. Both are single-occurrence today; the
# replacement is the longer sentence on the same line.
#
# Three more from their FlexCache verification, all single-occurrence at a97a4ec. The first two are
# constraints a reader of this repository's FlexCache section would hit at creation time -- a cache
# volume cannot be smaller than 50 GB, and the FSx for ONTAP aggregate is FabricPool-enabled so
# creation fails without opting into a tiered aggregate. The third is the teardown ordering that
# strands the only host able to fix it. `作成後の変更は ONTAP が拒否する` was **not** registered on
# their advice: it occurs twice there.
#
# **No figure is registered as a probe.** `1,135.18` occurs seven times in that file, so a probe on
# it would pass while the sentence around it changed. The probes are the claim-bearing sentences.
#
# NOT REGISTERED, DELIBERATELY: anything about iSCSI being stable across deployments. Their
# correction: iSCSI was measured in one deployment only, so there is no evidence either way, and
# "iSCSI was stable" must not become a reason to choose it here.
#
# Two more against their block runbook (new in their #160), which is where the limits on using their
# figures live rather than beside the figures. Both are `reread`, because both are expected to move
# when they measure more, and movement means this side should reread rather than that a claim broke:
#
#   - that a same-filesystem iSCSI vs NVMe/TCP comparison is among the things the procedure cannot
#     measure. **This is the line that decides how far the protocol comparison can be cited.** The
#     two protocols were separate runs, and the same NVMe/TCP configuration spread 2.64x across
#     environments, so the ratio between the protocols is not determined by these figures. If this
#     bullet disappears they have measured both on one filesystem, and the ratio becomes citable.
#   - their own instruction not to generalise the missing ANA multipath to "on AL2023". If it
#     disappears they have recorded the kernel version and the limitation can be stated precisely.
FSx-for-ONTAP-Adoption-Playbook	docs/ja/reference/comparison/block-to-file-routes.md	retraction	FlexClone が省くのは本番への影響です
FSx-for-ONTAP-Adoption-Playbook	docs/ja/reference/comparison/block-to-file-routes.md	retraction	アプリケーション側の保証は受けていません
S3-Burst-on-ONTAP-Files	docs/ja/verification/block-measurement-runbook.md	reread	「AL2023 では」と一般化しない
S3-Burst-on-ONTAP-Files	docs/ja/verification/block-measurement-runbook.md	reread	同一ファイルシステム上での iSCSI と NVMe/TCP の比較
S3-Burst-on-ONTAP-Files	docs/ja/verification/flexcache-security-style-inheritance.md	retraction	CloudFormation は削除を呼ぶ前にこの値を見て断る
S3-Burst-on-ONTAP-Files	docs/ja/verification/flexcache-security-style-inheritance.md	retraction	この種類のボリュームは最低 50 GB
S3-Burst-on-ONTAP-Files	docs/ja/verification/flexcache-security-style-inheritance.md	retraction	同じスタック削除が検証ホストを先に消す
S3-Burst-on-ONTAP-Files	docs/ja/verification/perf-matrix-results.md	reread	分母と分子が別のファイルシステムから来ており
S3-Burst-on-ONTAP-Files	docs/ja/verification/perf-matrix-results.md	retraction	1 → 2 では逐次スループットが 1 バイトも動かない
S3-Burst-on-ONTAP-Files	docs/ja/verification/perf-matrix-results.md	retraction	36 を要求しても 4 のままだった
S3-Burst-on-ONTAP-Files	docs/ja/verification/perf-matrix-results.md	retraction	9.08 Gbps を運んでおり、同じ上限には当たっていない
S3-Burst-on-ONTAP-Files	docs/ja/verification/perf-matrix-results.md	retraction	「1 接続」では iSCSI が速く、documented な手順のままでは NVMe/TCP が速い
S3-Burst-on-ONTAP-Files	docs/ja/verification/perf-matrix-results.md	retraction	この算術は両方向に外れる
S3-Burst-on-ONTAP-Files	docs/ja/verification/perf-matrix-results.md	retraction	どれが当たっているかを検証していないので、原因を書かない
S3-Burst-on-ONTAP-Files	docs/ja/verification/perf-matrix-results.md	retraction	セッション数を帯域の割り算で決める形そのものが成立していない
S3-Burst-on-ONTAP-Files	docs/ja/verification/perf-matrix-results.md	retraction	ファイルシステムを載せた場合の値ではない
S3-Burst-on-ONTAP-Files	docs/ja/verification/perf-matrix-results.md	retraction	幅は 2.64 倍である
